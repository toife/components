import { Direction } from './types';
export declare const cardinalFromDelta: (dx: number, dy: number) => Direction;
export declare const directionBeyondThreshold: (dx: number, dy: number, minMove: number) => Direction | undefined;
export declare const distance: (dx: number, dy: number) => number;
