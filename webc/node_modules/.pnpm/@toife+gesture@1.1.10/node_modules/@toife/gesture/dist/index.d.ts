import { GestureHandlers, GestureListenOptions } from './types';
export type { Direction, GestureDownPayload, GestureFastPayload, GestureHandlerOptions, GestureHandlers, GestureListenOptions, GestureMovePayload, GesturePhase, GestureUpPayload, } from './types';
export declare const gesture: (element: HTMLElement, handlers?: GestureHandlers, options?: GestureListenOptions) => {
    destroy: () => void;
    cancel: (event?: Event) => void;
};
