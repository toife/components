/** Elements that should receive normal click/tap without starting a gesture. */
export declare const DEFAULT_IGNORE_SELECTOR: string;
export declare const SUPPORTS_POINTER: boolean;
/**
 * Prefer touch listeners on touch-capable devices.
 * Pointer Events on mobile can be interrupted by browser gestures/scrolling
 * and cause swipe sessions to cancel unexpectedly.
 */
export declare const USE_POINTER_EVENTS: boolean;
