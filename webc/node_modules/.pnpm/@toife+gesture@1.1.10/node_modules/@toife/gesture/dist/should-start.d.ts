import { GestureHandlerOptions } from './types';
/**
 * Whether a pointer session may enter the pending phase.
 * Interactive descendants are skipped so clicks/taps still work.
 */
export declare const shouldStartGesture: (element: HTMLElement, event: Event, options?: Pick<GestureHandlerOptions, "handle" | "ignore">) => boolean;
