import { PropertyValues } from 'lit';
import { ModalProps } from '../../../../core';
import { ToifeElement } from '../../shared';
export declare class Modal extends ToifeElement {
    static readonly tagName = "t-modal";
    role: string;
    shape: string;
    visible: boolean;
    gesture: boolean;
    fullscreen: boolean;
    placement: string;
    keepalive: boolean;
    backdrop: string;
    indicator: boolean;
    duration: number;
    className: string;
    overlayStyle?: unknown;
    bounce: number | string;
    private isBusy;
    private isMoving;
    private presentEl?;
    private ges?;
    private modalEl?;
    private busyTimer?;
    connectedCallback(): void;
    disconnectedCallback(): void;
    updated(changed: PropertyValues): void;
    private get appState();
    private get gestureDir();
    private get modalAttrs();
    private onClose;
    private hasScrollableOverflow;
    private isScrollable;
    private hasRemainingScroll;
    private canStartGesture;
    private busy;
    private destroyGesture;
    private syncGesture;
    render(): import('lit').TemplateResult<1>;
}
export type { ModalProps };
