import { PropertyValues } from 'lit';
import { RouteNavigatorProps } from '../../../../../core';
import { ToifeElement } from '../../../shared';
import { RouterLike } from '../router-like';
export declare class RouteNavigator extends ToifeElement {
    static readonly tagName = "t-route-navigator";
    direction: string;
    variant: string;
    keepalive: boolean;
    gesture: boolean;
    name: string;
    router?: RouterLike;
    private stack;
    private activeIndex;
    private backdropIndex;
    private navigatorEl?;
    private transform;
    private ges?;
    private lastProviderStack?;
    connectedCallback(): void;
    disconnectedCallback(): void;
    firstUpdated(): void;
    updated(changed: PropertyValues): void;
    private get navigatorAttrs();
    private get componentAttrs();
    private get backdropAttrs();
    private get prevPage();
    private get isGestureEnabled();
    private emitTransform;
    private changeRoute;
    private commitGestureBack;
    private goBack;
    private resetTransform;
    private move;
    private up;
    private initGesture;
    render(): import('lit').TemplateResult<1>;
}
export type { RouteNavigatorProps };
