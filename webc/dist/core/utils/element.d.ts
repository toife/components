/**
 * Check if the target is a form element
 */
export declare const isFormElement: (target: EventTarget | null) => boolean;
/**
 * Blur the current active element
 */
export declare const blurCurrentActive: () => void;
