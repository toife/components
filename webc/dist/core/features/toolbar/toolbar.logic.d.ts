import { ToolbarAttrOptions } from './toolbar.type';
export declare const getToolbarAttrs: (options: ToolbarAttrOptions) => {
    class: (string | {
        safe: boolean;
        divider: boolean;
    })[];
};
