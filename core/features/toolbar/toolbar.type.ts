// Type definitions
export type ToolbarProps = {
  placement?: string | null;
  safe?: boolean;
  role?: string;
  divider?: boolean;
};

export type ToolbarAttrOptions = { role: string; placement: string; safe: boolean; divider: boolean };
